// cookiedclaw — Claude Code plugin (channels + skills + agents).
// Pivot in progress: the standalone Telegram-bot agent runtime that used to
// live here is replaced by a CC plugin that brings Telegram (+ image/memory/
// search integrations) on top of CC's own session. Source under construction.
